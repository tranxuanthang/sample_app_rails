<%= link_to gravatar_for(current_user, size: Settings.gravatar_list_size),
  current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".view_my_profile"), current_user %></span>
<span><%= t ".micropost_number", count: current_user.microposts.count %></span>
